import { makeStyles } from "@mui/styles"

export const useStyle=makeStyles(()=>({
    fullbetweenbox: {
        width: '100%',
        display: 'flex',
        justifyContent: 'space-between',
    },
}))